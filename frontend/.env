# API Configuration - Use empty string for production to enable relative API calls
# This allows the frontend to work with any domain through Nginx proxy
# For development, we use empty string since Vite proxy handles the routing
VITE_API_BASE_URL=

# Development settings
VITE_NODE_ENV=development

# Optional: Enable API request logging
VITE_API_DEBUG=true
